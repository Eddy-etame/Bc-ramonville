<!doctype html>
<html lang="fr" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Activités — Boxing Center Ramonville · anglaise, pieds-poings, grappling, MMA, Lady Punch & école dès 3 ans</title>
  <meta name="description" content="Toutes les disciplines de Boxing Center Ramonville : boxe anglaise & loisirs, pieds-poings, grappling, asso MMA dans l'octogone, Boxing Camp, Lady Punch 100 % féminin, école enfants dès 3 ans et accès libre muscu/cardio. Coach et créneaux réels pour chacune." />
  <meta name="keywords" content="cours de boxe Ramonville, grappling Toulouse, asso MMA octogone, Lady Punch Ramonville, boxe pieds-poings, baby boxe 3 ans, boxing camp, accès libre muscu Ramonville-Saint-Agne" />
  <meta name="robots" content="index, follow, max-image-preview:large" />
  <meta name="author" content="Boxing Center Ramonville" />
  <meta name="geo.region" content="FR-31" />
  <meta name="geo.placename" content="Ramonville-Saint-Agne — Toulouse" />
  <meta name="geo.position" content="43.546;1.474" />
  <meta name="ICBM" content="43.546, 1.474" />
  <link rel="canonical" href="https://ramonville.boxingcenter.fr/activites/" />
  <meta name="theme-color" content="#070a12" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=6" />

  <meta property="og:type" content="website" />
  <meta property="og:locale" content="fr_FR" />
  <meta property="og:site_name" content="Boxing Center Ramonville" />
  <meta property="og:title" content="Activités — Boxing Center Ramonville" />
  <meta property="og:description" content="Anglaise, pieds-poings, grappling, asso MMA dans l'octogone, Boxing Camp, Lady Punch et école dès 3 ans. Coach et créneaux réels." />
  <meta property="og:url" content="https://ramonville.boxingcenter.fr/activites/" />
  <meta property="og:image" content="https://ramonville.boxingcenter.fr/assets/img/ram/octogone.webp" />
  <meta property="og:image:alt" content="Séance d'asso MMA dans l'octogone de 7 m à Boxing Center Ramonville." />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Activités — Boxing Center Ramonville" />
  <meta name="twitter:description" content="Toutes les disciplines : boxe, MMA, grappling, Lady Punch, école dès 3 ans." />
  <meta name="twitter:image" content="https://ramonville.boxingcenter.fr/assets/img/ram/octogone.webp" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="preconnect" href="https://api.open-meteo.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@500;600;700&family=Manrope:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet" />
  <!-- base.css = le système (partagé) ; activites.css = le CSS PROPRE de la page -->
  <link rel="stylesheet" href="/assets/css/base.css?v=6" />
  <link rel="stylesheet" href="/assets/css/activites.css?v=6" />

  <script is:inline type="application/ld+json">
  { "@context":"https://schema.org","@type":"BreadcrumbList","itemListElement":[
    {"@type":"ListItem","position":1,"name":"Accueil","item":"https://ramonville.boxingcenter.fr/"},
    {"@type":"ListItem","position":2,"name":"Activités","item":"https://ramonville.boxingcenter.fr/activites/"}
  ]}
  </script>

  <script is:inline>
    (function(){try{var p=localStorage.getItem('bc-ram-palette');if(p==='cuivre')document.documentElement.setAttribute('data-palette','cuivre');}catch(e){}})();
    document.documentElement.classList.add('fx');
  </script>
</head>

<body data-page="activites">
  <a class="sr-only" href="#main">Aller au contenu</a>
  <div id="nav"></div>
  <div id="drawer"></div>

  <main id="main">
    <header class="phero" aria-label="Les activités">
      <div class="sky" aria-hidden="true"><canvas class="sky__stars"></canvas><div class="sky__haze"></div><div class="sky__moon"></div></div>
      <div class="wrap">
        <nav class="breadcrumb" aria-label="Fil d'ariane"><a href="/">Accueil</a> / <span>Activités</span></nav>
        <h1 class="display phero__title"><span class="reveal-mask"><span>Huit côtés.</span></span><span class="reveal-mask"><span class="tint">Une discipline chacun.</span></span></h1>
        <p class="lead phero__lead" data-reveal>Adultes, féminin, grappling &amp; MMA, école enfants dès 3 ans, accès libre. Chaque discipline avec son coach et ses vrais créneaux — filtre à gauche, et va droit à la tienne.</p>
        <!-- l'objet propre à ce hero (page.js → pheroMeta) -->
        <div class="phero__meta" id="phero-meta" data-reveal-group aria-label="Les 8 côtés, en chiffres"></div>
      </div>
    </header>

    <!-- ==================== RAIL D'INDEX + FLUX ==================== -->
    <section class="section" aria-label="Les disciplines">
      <div class="wrap">
        <div class="actwrap">
          <aside class="rail" aria-label="Sommaire des disciplines">
            <p class="rail__label">Filtrer</p>
            <div class="famfilter" id="famfilter" role="group" aria-label="Filtrer par famille"></div>
            <p class="rail__label">Les 8 côtés</p>
            <nav class="dindex" id="dindex" aria-label="Aller à une discipline"></nav>
          </aside>
          <div class="discs" id="discs"></div>
        </div>
      </div>
    </section>

    <section class="ctaband" aria-label="Essayer une discipline">
      <div class="sky" aria-hidden="true"><canvas class="sky__stars"></canvas><div class="sky__haze"></div></div>
      <div class="ctaband__inner">
        <span class="eyebrow" style="justify-content:center" data-reveal>Huit côtés, un seul essai</span>
        <h2 class="display"><span class="reveal-mask"><span>Un round et tu sauras lequel est le tien.</span></span></h2>
        <p class="lead" data-reveal>L'essai ouvre les huit. Tu changes d'avis autant de fois que tu veux.</p>
        <div class="ctaband__cta" data-reveal>
          <a class="btn btn--primary" data-magnetic href="https://box-plus.vercel.app/seance-essai"><span>Réserver l'essai · 10€</span></a>
          <a class="btn btn--ghost" data-magnetic href="/coachs/"><span>Voir les coachs</span></a>
        </div>
      </div>
    </section>
  </main>

  <div id="footer"></div>

  <a class="chatbot" href="tel:+33562244682" aria-label="Une question ? Appelle la salle Boxing Center Ramonville">
    <span class="chatbot__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.9.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92z"/></svg></span>
    <span class="chatbot__label">Une question&nbsp;? Appelle la salle</span>
  </a>

  <script is:inline src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script is:inline src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script is:inline src="https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.42/dist/lenis.min.js"></script>
  <script is:inline type="module" src="/assets/js/site.js?v=6"></script>
  <script is:inline type="module" src="/assets/js/sky.js?v=6"></script>
  <script is:inline type="module" src="/assets/js/page.js?v=6"></script>

  <!-- Enrichissement PROPRE à la page : le sommaire des 8 côtés + scrollspy.
       page.js a déjà rendu #famfilter et #discs (les fiches ancrées #<key>). -->
  <script is:inline type="module">
    import { DISCIPLINES } from "/assets/js/data.js?v=6";
    const reduce = matchMedia("(prefers-reduced-motion: reduce)").matches;
    const idx = document.getElementById("dindex");
    if (idx && DISCIPLINES) {
      // familles ordonnées (le libellé propre au sommaire, l'accès libre en dernier)
      const FAM = [
        ["adulte", "Adultes"], ["feminin", "Féminin"],
        ["mma", "Grappling · MMA"], ["enfant", "École enfants"], ["libre", "Accès libre"],
      ];
      const byEdge = [...DISCIPLINES].sort((a, b) => a.edge - b.edge);
      idx.innerHTML = FAM.map(([fam, label]) => {
        const items = byEdge.filter((d) => d.famille === fam);
        if (!items.length) return "";
        return `<div class="dindex__group"><span class="dindex__gk">${label}</span>${
          items.map((d) => `<a href="#${d.key}" data-key="${d.key}" data-fam="${d.famille}">
            <span class="di-name">${d.name}</span><span class="di-edge">${String(d.edge + 1).padStart(2, "0")}</span></a>`).join("")
        }</div>`;
      }).join("");

      const links = [...idx.querySelectorAll("a")];
      const allChip = document.querySelector('.chip[data-fam="all"]');

      // clic sommaire : on remet le filtre sur « Tout » (cible toujours visible) puis on scrolle
      idx.addEventListener("click", (e) => {
        const a = e.target.closest("a"); if (!a) return;
        e.preventDefault();
        const key = a.dataset.key;
        if (allChip && !allChip.classList.contains("is-on")) allChip.click();
        const el = document.getElementById(key);
        if (el) el.scrollIntoView({ behavior: reduce ? "auto" : "smooth", block: "start" });
        history.replaceState(null, "", "#" + key);
      });

      // le filtre famille estompe aussi les entrées du sommaire hors-cible
      const famfilter = document.getElementById("famfilter");
      if (famfilter) famfilter.addEventListener("click", (e) => {
        const b = e.target.closest(".chip"); if (!b) return;
        const fam = b.dataset.fam;
        links.forEach((a) => a.classList.toggle("is-dim", fam !== "all" && a.dataset.fam !== fam));
      });

      // scrollspy : surligne la discipline au centre du viewport
      if ("IntersectionObserver" in window) {
        const spy = new IntersectionObserver((es) => {
          es.forEach((e) => {
            if (!e.isIntersecting) return;
            const key = e.target.id;
            links.forEach((a) => a.classList.toggle("is-active", a.dataset.key === key));
          });
        }, { rootMargin: "-45% 0px -45% 0px", threshold: 0 });
        document.querySelectorAll(".disc").forEach((d) => spy.observe(d));
      }
      window.BC?.reveal(document);
      window.BC?.refresh();
    }
  </script>
</body>
</html>
